pre-commit:
  parallel: true
  commands:
    oxlint:
      glob: "*.{js,ts,vue}"
      run: bun oxlint --fix {staged_files}
    dprint:
      glob: "*.{js,ts,vue,json,md}"
      run: bun dprint fmt {staged_files}
