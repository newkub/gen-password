pre-commit:
  jobs:
    - name: format
      run: bun run format

pre-push:
  jobs:
    - name: build
      run: bun run build

pre-rebase:
  jobs:
    - name: fetch-origin
      run: git fetch origin

post-checkout:
  jobs:
    - name: install
      run: bun install
